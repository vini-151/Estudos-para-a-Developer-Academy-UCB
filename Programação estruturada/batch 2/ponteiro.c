#include <stdio.h>

int main()
{

    char *a, *b;
    a = "abacate";
    b = "uva";

        if(a < b)
        printf("%i vem antes de %i no dicionário", a, b);
    
    else
    
        printf("%s vem depois de %s no dicionário", a, b);
    
}